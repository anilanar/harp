import * as idom from 'incremental-dom';
import {
    ICache,
    Children,
    Template,
    Fragment,
    IOptions,
} from './types';

import {
    getFragment,
} from './fragment';


export {
    elementOpen,
    elementClose,
    elementVoid,
    text,
    patchOuter,
} from 'incremental-dom';

